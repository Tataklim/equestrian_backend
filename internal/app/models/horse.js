export const horseModel = (passport, moniker, sex, lear, country, breed, birth, image, passport_image, login) => {
    return {
        passport,
        moniker,
        sex,
        lear,
        country,
        breed,
        birth,
        image,
        passport_image,
        login
    };
};
