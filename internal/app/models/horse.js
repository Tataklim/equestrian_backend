export const horseModel = (moniker, sex, lear, country, breed, birth, image, passport_image, user_login) => {
    return {
        moniker,
        sex,
        lear,
        country,
        breed,
        birth,
        image,
        passport_image,
        user_login
    };
};
