export const horseModel = (moniker, sex, lear, country, breed, birth, image, passport_image, login) => {
    return {
        moniker,
        sex,
        lear,
        country,
        breed,
        birth,
        image,
        passport_image,
        login
    };
};
